apiVersion: v1
kind: Service
metadata:
  name: intervals-mcp
  namespace: mcp
  labels:
    app.kubernetes.io/name: intervals-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: intervals-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
